#ifndef HANDLEPATH_H
#define HANDLEPATH_H
#include <QString>


class HandlePath
{
public:
    HandlePath();
    ~HandlePath();
    int get_project_path();
    int get_xmldir_path();
public:
    QString m_project_path;
    QString m_sp_xml_path;
private:
    HandlePath(const HandlePath&);
    const HandlePath & operator = (const HandlePath&);
};

#endif // HANDLEPATH_H

